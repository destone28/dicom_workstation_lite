[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --cov=backend --cov=frontend --cov-report=html --cov-report=term
markers =
    integration: End-to-end integration tests with automatic server
    integration_manual: Integration tests requiring manual server startup
